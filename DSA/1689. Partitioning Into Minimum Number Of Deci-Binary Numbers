class Solution {
public:
    int minPartitions(string n) {
        int rem =0;
        int max_digit =0;
        for(char c : n){
            //int rem = n[i]-'0';
            if(max_digit< c-'0'){
                max_digit = c-'0';
            }
        }
        return max_digit;
    }
};
